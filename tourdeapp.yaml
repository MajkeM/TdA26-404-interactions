# $schema: https://portalbush.tourde.cloud/static/schema.json
exposedPort: 3000
containers:
  - name: server
    image: registry.tourde.cloud/guunrs/server
    port: 3000
build:
  - name: server
    context: ./apps/server
    dockerfile: ./apps/server/Dockerfile
